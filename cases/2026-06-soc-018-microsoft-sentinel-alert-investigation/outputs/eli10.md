# SOC-018 ELI10 Summary

This project is like checking the front door camera after someone typed the wrong door code several times.

At first, it looked suspicious because there were multiple failed sign-in attempts. Then the account finally signed in successfully.

The analyst checked whether anything bad happened after the login. The user completed MFA, no admin changes were found, and no suspicious activity happened afterward.

Final answer: suspicious at first, but no proof of compromise.
