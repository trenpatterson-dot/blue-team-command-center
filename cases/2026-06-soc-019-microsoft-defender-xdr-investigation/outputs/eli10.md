# SOC-019 ELI10 Summary

This project is like hearing an alarm because a computer used a powerful tool called PowerShell.

PowerShell can be normal when an admin uses it, but attackers can also use it to run commands.

The analyst checks what started PowerShell, what command ran, what happened next, and whether anything suspicious followed.

Final goal: decide if the activity was safe, suspicious, or dangerous.
